update

#--------------------------------
# Must install
#--------------------------------
install -y vim
install -y autojump
install -y tmux
install -y apt-file
install -y dtrx             # do the right extract
install -y cmake-curses-gui # ccmake command
install -y ruby
install -y rubygems         # ruby package manager

#--------------------------------
# GUI env tools
#--------------------------------
# install -y synaptic       # gui for apt-get
# install -y meld           # a diff tool ( http://meldmerge.org/ )
# install -y gitg           # git gui client tool
# install -y git-gui

#--------------------------------
# sometime used tools
#--------------------------------
# install -y htop           # a tool more convenient than top
# install -y tcsh
# install -y openvpn
# install -y ctags
# install -y cgdb
# install -y screen

#--------------------------------
# Bak (just memorize the name)
#--------------------------------
# install -y aptitude       # a text apt-get tool
# install -y byobu          # a convineint tool which backend is screen
# install -y mosh           # a remote terminal application ( http://mosh.mit.edu/ )
# install -y exuberant-ctags
# install -y openssh-server

# unetbooting dependency
# install -y ia32-libs
# install -y p7zip-full
